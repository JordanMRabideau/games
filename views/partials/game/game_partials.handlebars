<li>
	{{game_name}}
	<button class="change-played" data-id="{{id}}" data-newplayed="{{played}}">
		{{#if played}}Remove from 'played' list{{else}}Mark as played{{/if}}
	</button>
	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>
